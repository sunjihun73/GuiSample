<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.jihun.guisample.mapper.KnowledgeFileMapper">

    <resultMap id="knowledgeFileResultMap" type="kr.co.jihun.guisample.vo.KnowledgeFileVO">
        <id     property="fileId"        column="file_id"/>
        <result property="categoryId"    column="category_id"/>
        <result property="fileName"      column="file_name"/>
        <result property="updateUserId"  column="update_user_id"/>
        <result property="updateDate"    column="update_date"/>
        <result property="createUserId"  column="create_user_id"/>
        <result property="createDate"    column="create_date"/>
        <result property="categoryName"  column="category_name"/>
    </resultMap>

    <sql id="knowledgeFileWhere">
        <where>
            <if test="fileId != null and fileId != ''">
                AND kf.file_id = #{fileId}
            </if>
            <if test="categoryId != null and categoryId != ''">
                AND kf.category_id = #{categoryId}
            </if>
            <if test="fileName != null and fileName != ''">
                AND kf.file_name LIKE CONCAT('%', #{fileName}, '%')
            </if>
        </where>
    </sql>

    <select id="selectKnowledgeFileList"
            parameterType="hashmap"
            resultMap="knowledgeFileResultMap">
        SELECT
            kf.file_id,
            kf.category_id,
            kf.file_name,
            kf.update_user_id,
            kf.update_date,
            kf.create_user_id,
            kf.create_date,
            cm.category_name
        FROM
            knowledge_files kf
            LEFT JOIN category_master cm ON cm.category_id = kf.category_id
        <include refid="knowledgeFileWhere"/>
        ORDER BY kf.create_date DESC
        <if test="pageSize != null">
            LIMIT #{pageSize} OFFSET #{startRow}
        </if>
    </select>

    <select id="countKnowledgeFile"
            parameterType="hashmap"
            resultType="int">
        SELECT
            COUNT(*)
        FROM
            knowledge_files kf
        <include refid="knowledgeFileWhere"/>
    </select>

    <insert id="insertKnowledgeFile"
            parameterType="kr.co.jihun.guisample.vo.KnowledgeFileVO">
        INSERT INTO knowledge_files (
            file_id,
            category_id,
            file_name,
            create_user_id,
            create_date,
            update_user_id,
            update_date
        ) VALUES (
            #{fileId},
            #{categoryId},
            #{fileName},
            #{createUserId},
            NOW(),
            #{updateUserId},
            NOW()
        )
    </insert>

</mapper>
